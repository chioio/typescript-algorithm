/**
 * Raise number to the power.
 *
 * * Time complexity: O(1)
 * * Space complexity: O(1)
 *
 * @param number a number
 * @param power numbers' power
 * @returns power calculate result
 */
export function fastPower(number: number, power: number): number {
  return number ** power;
}
